# Core Flask dependencies
Flask>=2.3.0
Flask-CORS>=4.0.0

# Image processing (lightweight)
Pillow>=9.0.0

# Core ML libraries (optimized versions)
torch>=2.0.0
torchvision>=0.15.0

# Transformers (essential only)
transformers>=4.35.0

# YOLO (latest stable)
ultralytics>=8.0.0

# Utilities
numpy>=1.24.0
requests>=2.31.0

# Production WSGI server
gunicorn>=21.0.0

# Optional: Remove heavy dependencies for faster deployment
# opencv-python>=4.8.0  # Commented out - use Pillow instead
# tqdm>=4.65.0  # Commented out - not essential for inference
